server.port=8080
spring.datasource.url= <url>
spring.datasource.username=<user>
spring.datasource.password=<password>

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.properties.hibernate.use_sql_comments=true
logging.level.org.hibernate.type=trace
spring.jpa.properties.hibernate.type=trace

EXPIRATION_DURATION_IN_MS=<exp>
token.secret.key= <pass>
REFRESH_TOKEN_VALIDITY_MS=60400000
mail.username=<email>
mail.password=<pass>
mail.host=smtp.gmail.com
mail.port=587
mail.transport.protocol=smtp
mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.debug=true
password.reset.validity.hours=24

bearer.size=7
# DEV URL
front-end.server.url=localhost:3000

signup.verification.token.hours=5000
password.minimum.length=8

# Quartz config
spring.quartz.job-store-type=memory

max.retries = 5
email.scheduler.interval.seconds=600

# Stripe
stripe.api.public.key=<stripe pk>
stripe.api.secret.key=<stripe sk>


spring.security.csrf.enabled=false

# URL
base.url.backend=http://localhost:8080
base.url.frontend=http://localhost:3000



